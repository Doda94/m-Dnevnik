<?xml version="1.0" encoding="utf-8"?>
<navigation
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/loginFragment">
    <fragment
        android:id="@+id/loginFragment"
        android:name="com.doda.e_dnevnik.ui.LoginFragment"
        android:label="fragment_login"
        tools:layout="@layout/fragment_login">
        <action android:id="@+id/action_loginFragment_to_dataFragment"
            app:destination="@id/dataFragment"/>
    </fragment>
    <fragment
        android:id="@+id/dataFragment"
        android:name="com.doda.e_dnevnik.data.DataFragment"
        android:label="fragment_data"
        tools:layout="@layout/fragment_data">
        <action
            android:id="@+id/action_dataFragment_to_razrediFragment"
            app:destination="@id/razrediFragment"/>
    </fragment>
    <fragment
        android:id="@+id/razrediFragment"
        android:name="com.doda.e_dnevnik.razredi.RazrediFragment"
        android:label="fragment_razredi"
        tools:layout="@layout/fragment_razredi">
        <argument
            android:name="id"
            app:argType="string"/>
    </fragment>
</navigation>